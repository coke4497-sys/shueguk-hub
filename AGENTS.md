# AGENTS.md — 슈국 허브 (shueguk-hub)

학원 티쳐스(교사) 허브 페이지입니다. `index.html`(메인 메뉴)에서 각 시스템으로 이어지고, 이 저장소에 본체가 있는 화면은
모의고사 출제·OMR·성적·회차 분석(`answer_key.html`·`omr.html`·`omr_teacher.html`·`omr_analysis.html`),
클리닉 질문 대기열(`question.html` 교사 순서 정하기 · `question_board.html` 강의실 전자칠판 · `question_guide.html` 안내)입니다.
GitHub Pages로 배포됩니다(`coke4497-sys.github.io/shueguk-hub/`). 자세한 이력은 리포트 저장소(`shueguk-report/CLAUDE.md`)에 있습니다.

## Review guidelines

- 리뷰 댓글은 **한국어**로, 비개발자(학원 원장)도 읽을 수 있게 쉬운 말로 씁니다.
- 교사 인증 조각이 학생에게 노출되는 문제, 채점·집계가 어긋나는 문제를 최우선으로 봅니다.
- 아래 규칙에 어긋나는 변경은 반드시 지적하고, 취향 차이는 지적하지 않습니다.

## Code Review Rules

### 인증·데이터
- 교사 페이지들(`omr_teacher.html`·`omr_analysis.html`·`question.html`·`question_board.html`)은 **조용한 교사 인증 조각이 첫 스크립트 자리**에 있어야 수파베이스 표를 읽습니다. 이 조각을 학생용 페이지(`omr.html` 등)나 공개 안내(`question_guide.html`)에 넣는 변경은 지적하세요. `question_guide.html`은 링크 공유용이라 인증 조각이 없어야 합니다.
- 회차·응답의 원본은 수파베이스(`omr_exams`·`omr_responses`)입니다. 시트를 원본으로 되돌리는 변경은 지적하세요.
- 회차 분석의 점수·등급은 저장값이 아니라 **지금 정답으로 다시 채점한 값**입니다. 집계 로직은 `ANALYSIS-CORE` 블록 한 곳이고 `tools/omr-analysis-test.js`가 그 블록을 검증합니다. 로직 변경에 테스트 갱신이 없으면 요청하세요.
- 질문 대기열(`question_queue`)을 순회하는 코드는 **`일시정지` 상태 줄을 걸러내야** 합니다(교사 안내 문구·일시 정지용 줄). 상태 이름 `일시정지`·`예약`·`명단`·`대기`·`호출`·`질문중`·`완료`·`취소`·`건너뜀`은 바꾸지 않습니다(스키마 없이 문자열로 씀).
- `tt_classes`를 새로 읽는 코드는 `class_id=not.like.w*`로 '이 주만' 반을 뺍니다.
- 교사 화면의 [질문 대기]·전자칠판 도착 PATCH는 `status=in.(예약,명단)` 조건으로 이미 줄 선 학생을 되돌리지 않습니다. 조건을 빼는 변경은 지적하세요. 재호출은 `status=eq.호출` 조건으로 `called_at`만 바꿉니다.
- `sb()` 응답 처리는 빈 본문(201/204)도 받아야 합니다(`r.text()` 경유). `r.json()`으로 바꾸면 일괄 INSERT가 성공하고도 오류로 보입니다.
- 사진은 목록 조회에 싣지 않고 처음 보는 건만 `select=id,photo`로 따로 받습니다(폴링에 매번 실으면 무거움).

### 화면·디자인 (사용자 확정 사항 — 되돌리지 말 것)
- 폰트는 고운 바탕·고운 돋움·도현체만(도현체·고운돋움은 굵기 하나라 700 금지). 워드마크 Shueguk은 새크라멘토. UI에 컬러 이모지 아이콘 금지.
- 전자칠판(`question_board.html`)은 **고정 1520×856 캔버스를 통째로 축소·확대**(transform scale)합니다. vw/vh 리플로우로 바꾸지 않습니다. 이름 도현체는 **88px 상한**(키우지 말 것). **이름에 초록색을 쓰지 않습니다**(세이지는 제목·버튼에만). 학생·반별로 다른 색 카드 금지.
- 교사 페이지(`question.html`)에 진한 녹색 채움 버튼 금지 — 버튼은 연보라(#EDE4F4/#8D83A8) 바탕만, 테두리 없음. 화면 아래 고정 리모컨 줄(`#mbar`)은 사용자가 뺀 것이니 되살리지 않습니다.
- 기능을 없애는 방식의 디자인 변경은 지적하세요(사용자 지시 "기능을 없애기보다 디자인적 요소를 반영").
- 질문 대기열의 버튼 이름·기능이 바뀌면 `question_guide.html`(선생님용 안내)도 함께 바뀌어야 합니다.
- 통합형/고3형 출제 두 형식을 병행합니다(기본값 고3형). 한쪽을 깨는 변경은 지적하세요.

### 테스트
- `tools/qq-e2e-test.js`(질문 대기열 E2E)·`tools/omr-analysis-test.js`가 있습니다. 관련 변경에 테스트 갱신이 없으면 알려 주세요.
