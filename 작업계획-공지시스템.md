# 작업 계획 — 학생 선택 공지/배정 시스템

> 작업 인계용 메모 (사이트에는 표시 안 됨). 다시 시작할 때 이 순서대로.

---

## 재배포 방법 (백엔드 바꿀 때마다 사장님이 1회)

대상: **지필고사 리포트 구글시트**의 Apps Script
1. 시트 → 확장 프로그램 → Apps Script
2. 기존 코드 전부 지우고 → `shueguk-report/backend-createReport.gs` 최신본 붙여넣기 → 저장
   - Raw: https://raw.githubusercontent.com/coke4497-sys/shueguk-report/main/backend-createReport.gs
3. **배포 → 배포 관리 → (기존 배포) 편집(연필) → 버전 "새 버전" → 배포** (URL 유지)
4. 확인: `…/exec?action=roster` 에서 학생 명단 JSON이 나오면 성공

---

## ✅ 완료된 것

**공용 학생 선택 위젯** — `shueguk-report/student-picker.js` + `.css`
- 모드: 전 학년 / 학년(여러 학년) / 개인 / 일부
- 일부 탭에 **학년·학교 필터 + "보이는 학생 모두 선택"** → 특정 학년+특정 학교만 한 번에 선택
- 사용법:
  ```html
  <link rel="stylesheet" href="https://coke4497-sys.github.io/shueguk-report/student-picker.css">
  <div id="picker"></div>
  <script src="https://coke4497-sys.github.io/shueguk-report/student-picker.js"></script>
  <script>
    var currentSel = null;
    var picker = StudentPicker.create(document.getElementById('picker'), {
      endpoint: REPORT_API,            // 리포트 백엔드 .../exec
      onChange: function(sel){ currentSel = sel; }
    });
    // 제출 시: var sel = currentSel || picker.getSelection();  // {type, target, count, summary}
  </script>
  ```
  ⚠️ **반드시 onChange의 currentSel을 쓸 것** (picker.getSelection() 직접 호출이 일부 환경에서 null 반환하던 이슈 회피 — 검증됨)

**중앙 배정 저장소** (리포트 시트, 모든 도구 공용)
- `배정` 탭: A작성일 B도구 C항목 D대상유형 E대상 F마감 G비고
- 백엔드 액션: `addAssignment` / `assignList?tool=&item=` / `deleteAssignment` (pw=`sh` 필요)
- 항목(item) 규칙: H WORK = `"강사 / 제목"`

**공지(알려드립니다)** — `notice.html` + 백엔드 공지 CRUD + 슈국 티쳐스 "공지 보내기" 카드. 입력+노출/숨김+삭제.

**H WORK 배정·관리 (표준 템플릿, 검증 완료)**
- `shueguk-h-work/hwork_assign.html`: 과제 선택 → 학생 배정 → 제출/미제출 현황(이름+학교 매칭, 동명+동학교는 ⚠️ ID확인)
- 허브 `shueguk-hub/hwork.html`에 "배정·현황" 카드
- 학생 개인 페이지 `shueguk-report/s.html`: 'H-work' 카드가 **그 학생에게 배정된 과제만** 표시 (백엔드 `collectAssignments_` + resp.homework)

---

## ✅ 추가 완료 (배정·관리 + 학년 설정)
- **클리닉 배정·관리**: `shueguk-clinic/clinic_assign.html` (배정 + 이번 주 신청/미신청). 허브 클리닉 카드.
- **주말 모의고사 신청 가능 학년**: `signup_code.gs`(grades, 기본 고3) + `signup.html`(학년 동적) + `signup_teacher.html`(학년 토글)
- **클리닉 신청 가능 학년**: `Code.gs`(grades, 기본 고3) + `teacher.html`(학년 토글)
- **학생 페이지 학년 게이팅**: `s.html` — 모의고사·클리닉 카드를 학생 학년 vs 열린 학년으로 게이팅(미해당 시 '운영 안 함' + 링크 비활성)
- 백엔드 재배포 완료: 지필고사 리포트 / 주말 신청 / 클리닉 (3개 모두)

## ✅ 추가 완료 — 지필고사 리포트(분석지) 시스템
- **허브 3메뉴**: `shueguk-hub/report.html` — ①제작(m.html) ②수정·배정(analyses.html) ③학생작성 확인(t.html)
- **제작+배정 통합**: `m.html` — 등록 시 student-picker로 학생 배정(전체/학년/개인/일부),
  저장 버튼 하나가 리포트+배정을 함께 저장. `?id=` 로 열면 수정 모드(제목=ID 잠금, 원래 ID 유지 → 중복 방지)
- **수정·배정 관리**: `analyses.html` — 리포트 목록+현재 배정 표시, 수정 버튼(→m.html?id=), 배정 해제, **리포트 삭제**
  (백엔드 `deleteReport`: 보고서목록·문항·배정 제거, 제출결과=학생 복기는 보존)
- **배정 저장 방식**: `배정` 탭에 도구=`지필고사 분석지`, 항목=시험ID. 학교·학년 자동배정은 선택과목 문제로 **폐기** — 교사 배정만 유효
- **학생 페이지**: 분석지 복기는 별도 카드가 아니라 **'알려드립니다'에 통합** (미입력=빨간 뱃지+복기 입력 버튼 맨 위, 완료=아래)
- **복기 입력**: `r.html` — 상단 시험 제목 표시, 리포트 이미지(빈 분석지 PNG) 저장, 제출 응답 검증(실패 시 오류 표시)

## ✅ 슈퍼스타 별(경험치) 시스템 — 완료
- **개념**: 별 누적으로 12단계 승급(원석0 → … → 슈퍼스타80). 임계값은 `s.html`의 `LEVELS`,
  적립 규칙은 리포트 백엔드 `STAR_RULES` 상수 (지필×2 / 클리닉신청·어휘·과제·공지확인 ×1 / 보너스 수동)
- **자동 집계**: 리포트 백엔드 `collectStars_`가 제출결과·클리닉시트·어휘시트(`VOCA_SHEET_ID`)·
  H WORK시트(`HWORK_SHEET_ID`, 탭 `제출기록`)를 읽어 합산 → getStudent 응답 `stars`
- **학생 페이지**: '나의 슈퍼스타 여정' 카드(등급·별 진행·사다리·보너스 알림)
- **깜짝 보너스**: `star.html`(허브 '슈퍼스타 별 주기' 카드) → `별` 탭에 로그, 삭제 가능
- **공지 확인**: 알려드립니다 공지마다 '확인했습니다' 버튼 → `공지확인` 탭 기록, 별 +1,
  뱃지는 미확인 공지만 카운트 (읽지 않음 표시 과제 해결)
- **H WORK 제출 완료 표시**: 학생 페이지 과제 목록이 `제출기록`과 대조해 '✓ 제출 완료' 전환
- H WORK 백엔드 원본 백업: `shueguk-h-work/apps-script/Code.gs` (7월 사고 복구 후 보관)

## ✅ 지필고사 제출 분석 대시보드 — 완료
- `shueguk-report/stats.html` (허브 report.html 4번 메뉴 '제출 분석')
- 시험 선택 → 제출 인원·평균·최고, 학교학년 필터 칩, 많이 틀린 문항 TOP5, 학생·점수 표
- 백엔드 추가 없음 (?list=1, ?results= 재사용). 복기 점수는 소수점 입력 가능(r.html step=any)

## ✅ 2026-07 전체 시스템 검토 반영
- 클리닉 백엔드: **토큰·학년·학생ID·담당강사 기록** 추가(ensureHeaders_) → s.html '내 클리닉 신청' 작동,
  학년 미허용 시 `grade_closed` 거부 + `index.html` 학년 선택 필드. **→ 클리닉 재배포 필요**
- 어휘 백엔드 `checkTaken_`: 전화4 불일치로 응시 기록을 놓치지 않게 완화(이름+주차 우선). **→ 어휘 재배포 필요**
- 알려진 잔여: 레거시 `shueguk-vocab-1~5-FINAL.html`(고아, 다른 백엔드) /
  `shueguk-me`는 빈 저장소 / 비밀번호(sh·shueguk2026)가 공개 소스에 노출되는 구조적 한계
  (H WORK 백엔드는 `shueguk-h-work/apps-script/Code.gs`로 백업 완료)
- **아카이브 예정(1~2년 뒤)**: 별 계산 등이 시트 전체를 매번 읽는 구조라 제출 기록이 수만 줄 쌓이면
  학생 페이지가 느려짐 → 지난 학년도 기록(제출결과·제출기록·어휘 결과 등)을 별도 시트로 옮기는
  아카이브 도구 제작 필요. 사장님이 "느려졌다"고 하면 이 항목을 진행할 것.
  참고: Apps Script 동시 실행 약 30명 제한 — 전체 공지 문자는 반별 시차 발송 권장.

## ✅ 2026-07 주말 모의고사 ↔ 학생 개별 페이지 매칭 보강
- **매칭 규칙 확정**: 이름+학교 1차(느슨 비교, `schoolMatch_`), 학생ID(부모님 8자리)는 **동명이인 구분 보조**로만.
  쌍둥이는 부모님 번호가 같으므로 ID를 1차 키로 쓰면 안 됨(이름이 달라 1차에서 분리됨).
- `shuegukweekendtest/omr_code.gs` v3: `studentReports_` 매칭 교정 + `schoolMatch_` 추가. **→ OMR 재배포 필요**
- 학생 OMR 화면에 '학부모님 연락처 8자리' 입력 + `payload.studentId` 추가 (응답 시트 '학생ID' 열에 기록).
  ⚠️ 실제 학생 화면은 Apps Script 안의 `omr_student` 파일 — `shuegukweekendtest/omr_student.html`(실파일 기준으로
  수정 반영한 원본 사본)을 **통째로 붙여넣으면 됨**. 이후 omr_student 수정은 이 파일을 기준으로 할 것.
  (`index.html`은 옛 HTTP 방식 참고본 — 실서비스와 다름)
- `shueguk-report/s.html`: 모의고사 잠금이 리포트 백엔드 `&pw=` 검증(학생정보 대조) 통과 후에만 조회하도록 변경
  (기존엔 아무 8자리나 통과되던 문제). 정적 파일이라 머지만으로 반영.
- 옛 응답(학생ID 빈칸)은 이름+학교로 계속 조회됨(소급 문제 없음). 동명이인+동학교의 옛 기록만 구분 불가.

## ✅ 2026-07 주말 모의고사 신청 — 중복 신청 삭제 기능
- 배경: 학생·학부모 모두에게 발송된 안내로 같은 학생이 중복 신청하는 사례 발생
- `signup_code.gs`: `action=delete`(pw 필요) — 행 번호+이름+제출시각 대조 후 삭제(목록이 낡았으면 stale 반환),
  `action=data` 응답에 `_row`(시트 행 번호) 추가. **→ 신청 백엔드 재배포 필요**
- `signup_teacher.html`: 표에 '삭제' 버튼(확인 창 → 삭제 → 자동 새로고침) +
  같은 주차·같은 학생(이름+학교) 2건 이상이면 이름 옆 **'중복' 뱃지** 표시. 정적 파일 — 머지만으로 반영.
- **일괄 삭제**: 체크박스로 여러 건 선택 → '선택 N건 삭제'. 백엔드 `action=deleteMany`
  (큰 행부터 삭제해 행 밀림 안전, 대조 실패분은 건너뜀). **→ 신청 백엔드 재배포 필요**
- 응시일자는 텍스트(') 저장 + 화면은 주차·요일에서 'M월 D일' 계산 (시트 날짜 자동변환 문제 해결)

## ✅ 2026-07 학생 페이지 링크 검색 (티쳐스)
- 허브 메인 '학생 페이지 링크' 카드 → `shueguk-report/links.html` (비밀번호 게이트)
- 백엔드 `action=studentLinks`(pw 필요) — 재원생 이름·학교·학년·담당·**접근코드** 반환 (공개 금지 데이터라 pw 필수)
- 검색·필터 후 학생별 '링크 복사'/'열기'. 접근코드 빈 학생은 assignAccessCodes() 실행 안내
- **→ 리포트 백엔드 재배포 필요** (아래 재배포 대기 ①에 포함됨)

## ✅ 2026-07-11 재배포 완료 (리포트·신청·OMR 3곳)
- **이름이 유일한 학생은 신청서·OMR의 8자리 오타 허용** (uniq 신호 — 동명이인만 엄격 대조)
  + 별 집계(countMock_) 동일 적용 + **학생 페이지 링크 검색(studentLinks)** 모두 반영됨.
- (참고) 2026-07-11 송민혁 신청 내역 미표시 건의 실제 원인은 학생정보 탭 학교 오타('헹신고')였음 — 시트 수정으로 해결.
  ⚠️ 학생정보 탭의 학교명 오타는 모든 매칭에 영향 — 이상하면 학생정보 학교부터 확인할 것.

## ✅ 2026-07 학생 페이지 모의고사 신청 내역 표시
- `signup_code.gs`에 `action=mySignups&name=&school=&id=` — 그 학생의 이번 주 신청(요일·응시일)만 반환.
  매칭 규칙은 성적 조회와 동일(이름+학교 느슨, ID는 동명이인 보조). **→ 신청 백엔드 재배포 필요**
- s.html: 실전 모의고사 하위 메뉴 신청 카드에 '이번 주 신청 완료 ✓ · 토요일 (M월 D일)' 표시,
  메인 카드 설명에도 반영. 백엔드 재배포 전에는 기존과 동일하게 동작(안전).

## ✅ 2026-07 학생 페이지 '실전 모의고사' 하위 메뉴 + 응시 별 적립
- s.html 메인 메뉴: 모의고사 신청/OMR/성적 3개 카드 → '실전 모의고사' 카드 1개 + 하위 메뉴로 통합
- 별 규칙에 **모의고사 응시 ×1** 추가 (`STAR_RULES.mock`, 회차 단위 — 같은 회차 중복 제출 1개).
  리포트 백엔드 `OMR_SHEET_ID`(응답 탭) 자동 집계, 개인·TOP10 모두 반영. **→ 리포트 백엔드 재배포 필요**

## ✅ 2026-07 학생 개별 페이지에서 OMR 작성
- s.html에 '모의고사 OMR' 카드 — 이름·학교·학년·학부모8자리를 nm·sc·gr·sid 파라미터로 실어
  허브 `omr.html`로 이동 → omr.html이 iframe(exec) 주소에 그대로 전달 →
  `omr_student`가 google.script.url.getLocation 으로 읽어 **자동 입력** (오타로 인한 매칭 실패 차단)
- 반영 조건: s.html·omr.html은 머지만으로 반영(정적).
  **omr_student는 Apps Script에 `shuegukweekendtest/omr_student.html` 최신본 재붙여넣기 + OMR 재배포 필요**

## ✅ 2026-07 클리닉 역할 재배치 (설정은 배정·현황, 신청확인은 내역 전용)
- **배정·현황(clinic_assign)**: 신청받기 토글 + 시간대 편집 + 학생 배정. **배정 저장 = 신청 대상 설정**
  (같은 선택이 클리닉 백엔드 target으로 저장 → 그 학생들만 신청 가능·개인 페이지 카드 표시)
- **신청 확인(teacher.html)**: 신청 내역 조회 전용 (설정 패널 전부 제거)
- Code.gs `action=amITarget` — s.html이 본인 대상 여부만 조회(true/false), 카드 게이팅에 사용
- '신청 가능 학년' 개념은 신청 대상으로 완전 통합 (대상 미설정 시 기본 고3 승계). **→ 클리닉 재배포 필요**
- clinic_assign 잠금 비밀번호 c2g=(sh) 재적용 (이전 통일 때 되돌아가 있던 것 교정)

## ✅ 2026-07 클리닉 — 시간대 직접 편집 + 신청 대상 설정
- 교사 페이지(teacher.html): 시간대 목록 **직접 추가·삭제**(마스터가 Script Properties `slotMaster`로 이동,
  코드 수정 불필요) + **신청 대상 패널**(student-picker — 전 학년/학년/개인/일부, 일부 탭은 학년·학교 필터)
- Code.gs: `setSlotMaster`(마스터+열 시간대 동시 저장) / `setTarget` / 제출 시 `eligibleForTarget_` 검증.
  **개인·일부 지정은 학년 설정보다 우선** (지정된 학생은 학년 무관 신청 가능). 동명이인 토큰(이름|학교|학년) 지원.
  slots 응답에 targetType 추가. **→ 클리닉 재배포 필요**
- index.html: 대상 아님(not_target) 안내 / s.html: 대상이 개인·일부면 클리닉 카드 학년 게이팅 우회

## ✅ 2026-07-15 어휘 주차 되돌아감 사건 (2→1) — 원인 추적 + 안전장치
- 사실관계: 낮에 1→2로 변경(정상 저장, 학생들이 2주차 응시) → 시트 수정 기록상 **오후 7:03에 "2"→"1"로 교체**
  → 이후 학생들이 1주차를 봄. 스크립트가 소유자 권한으로 쓰므로 수정자 표시로는 페이지 저장인지 수동 편집인지 구분 불가.
- 유력 경로 둘: ① 다른 기기/탭에 열려 있던 **낡은 어휘 티쳐스 화면**에서 드롭다운 저장 ② 시트에서 **실행취소(Ctrl+Z)**
  (실행취소는 스크립트가 쓴 값도 되돌림).
- 안전장치(적용 완료):
  - 허브 `voca.html`: 주차 변경 시 **저장 전에 서버 현재값 재확인** — 화면이 낡았으면 저장 대신 갱신+안내,
    같으면 확인 창("N주차 → M주차 변경할까요?") 후 저장. 탭이 다시 보이면 자동 재로딩. (정적 — 반영됨)
  - 리포트 백엔드: `setConfig_` 중복행 방지 보강 + **`설정로그` 탭**(일시·항목·값) — setVoca/setVocaWeek가
    저장할 때마다 기록되어 다음부터는 "언제 무엇이 스크립트로 저장됐는지" 추적 가능. **→ 리포트 재배포 필요**
  - `shueguk-voca/test.html`: 서버 주차와 다른 주차 링크는 **응시 차단**(지난 주차 직접 접속 불가). (정적 — 반영됨)
- 사장님 습관 권장: 어휘 티쳐스 페이지를 여러 기기/탭에 켜두지 말 것, 설정 탭에서 Ctrl+Z 하지 말 것.

## ✅ 2026-07-15 별 부풀림 수정 (김연서 별 11개 사건)
- 원인: 별 집계가 지필·과제를 **제출 행 수**로 세어, 같은 리포트를 여러 번 제출하면 별이 그만큼 늘어남.
- 수정(개인 페이지 + TOP10 순위 동일 적용): 지필 = **시험명 단위** ×2 / 과제 = **강사|제목 단위** /
  클리닉 = **신청 주(월요일 시작)|시간대 단위** (기존 키의 제출시각은 매번 달라 중복을 못 걸렀음) /
  공지 확인(순위 쪽) = 공지키 단위. 어휘(주차)·모의고사(회차)는 원래 정상.
- 클리닉 매칭 보강: 토큰 있는 행은 토큰으로만 (쌍둥이 학생ID 공유로 형제 신청까지 잡히는 것 예방).
- 시트의 중복 행은 지워도 안 지워도 별에 영향 없음(소급 재계산). **→ 리포트 백엔드 재배포 필요**
- 추가 수정 ①(박하율 순위 누락): TOP10 매칭이 개인 페이지보다 엄격했음 → **이름이 명단에서 유일하면
  학교 표기 차이 허용, 동명이인은 같은 이름 전원 대상 학생ID→학교 구분** (기존엔 첫 번째 학생만 비교).
- 추가 수정 ②(동점 순서): 별 개수 같으면 가나다순 → **그 별 수에 먼저 도달한 순**
  (단위별 첫 제출 시각 기준, 중복 재제출은 도달 시각에 영향 없음). **→ ①②는 재배포 한 번에 포함**

## ⬜ 남은 일

### (선택) 주말 모의고사 배정·관리 — H WORK/클리닉 템플릿 복제
- 필요 시 `shuegukweekendtest`에 배정 페이지(tool='주말모의고사') + 신청/응시 대조. (현재는 학년 설정으로 신청 범위 제어 중)

### 학생용 페이지 "읽지 않음" 표시 — ✅ 전부 완료 (2026-07 확인)
- [x] 어휘 테스트 미응시 뱃지 (어휘 백엔드 `action=taken` / s.html `STATE.vocaTaken`)
- [x] 지필고사 분석지 미입력 뱃지 (알려드립니다 카드에 합산)
- [x] 공지 미확인 뱃지 — '확인했습니다'(공지확인 탭) 기준으로 미확인만 카운트 (s.html nCnt)
- [x] H WORK 미제출 뱃지 — '풀어야 할 과제 N건' + 빨간 점 (제출기록 대조, s.html hCnt)

### ✅ 2026-07 OMR 중복 제출 삭제 — 성적 대시보드에 통합
- 별도 페이지(omr_admin) 대신 **성적 대시보드(Apps Script teacher.html) 안에서 삭제** 방식으로 변경
- 백엔드: `deleteResponseRun(pw,row,name,ts)` (google.script.run용) + HTTP `responses`/`deleteResponse` 액션
- teacher.html 수정은 omr_student와 동일하게 **저장소 사본 기준 통째 교체** 방식. **→ OMR 재배포 필요**

## 코드 복사 도우미 (hub/copy.html) — 2026-07
- 티쳐스 메인 카드 '코드 복사 도우미'. 버튼 한 번으로 GitHub main의 **최신 코드**를 클립보드에 복사
  (모바일 재배포용). 6종: 리포트 백엔드 / OMR 백엔드 / omr_student / teacher(성적 대시보드) / 신청 백엔드 / 클리닉 백엔드.
- ⚠️ **백엔드 파일을 추가·이동·개명하면 copy.html의 FILES 목록도 반드시 함께 갱신할 것.**
  저장소가 public이어야 동작(raw.githubusercontent.com에서 가져옴).

## 퇴원 정리 도구 (report/exit.html) — 2026-07
- 티쳐스 메인 카드 '퇴원 정리'. 이름(+선택 8자리)으로 **9개 시트**(학생정보·제출결과·별·공지확인·
  OMR 응답·모의고사 신청·클리닉·어휘·H WORK)에서 기록을 찾아 체크 목록 → 확인 → 일괄 삭제.
- 8자리를 넣으면 번호가 다른 동명이인 행은 자동 체크 해제("번호 다름" 표시) — 오삭제 방지.
- 학생정보 행 삭제 = 개인 페이지 링크 즉시 무효화. 운영 방침: 퇴원 시 기록까지 삭제(개인정보 보호).
- 백엔드 exitScan/exitDelete (pw 게이트, 이름 재대조·큰 행부터 삭제·300건 제한). SIGNUP_SHEET_ID 상수 추가.

## 시스템별 백엔드 시트 (재배포 위치)
- 지필고사 리포트 시트 → 공지·배정·학생명단 (`backend-createReport.gs`)
- 주말 모의고사 신청 시트 → 신청·학년 (`signup_code.gs`, SHEET_ID 코드에 박힘)
- 클리닉 응답 시트(1q-D…) → 클리닉 신청·시간대·학년 (`Code.gs`)

---

## 참고 / 주의
- **별·성적 매칭 최종 규칙 (2026-07 확정)**: ① 부모님 8자리+이름 정확 일치 → 무조건 본인
  ② 접미사 뗀 이름이 명단에서 유일 → 학교·번호 오타 허용, 단 **학년 모순이면 명단 밖 동명이인으로 보고 제외**
  ③ 동명이인(접미사 가족 포함, 이승준A/B/C·김윤우A/B) → 번호 → 학교+학년 → 학교 순 엄격 대조,
  기록의 번호가 본인과 다르면 제외. **같은 학교·같은 학년 동명이인은 번호로만 구분되므로 8자리 필수.**
  접미사 학생의 실명 제출('이승준')은 자동으로 가족 중 맞는 학생에게 배분. 개인 페이지·TOP10 동일 규칙.
  검증: 모의 시트 시뮬레이션 `scratchpad/test-rank.js`(세션 임시) — 시나리오: 중복 제출·학교 오타·
  접미사 3형제·같은 학교·학년 동명이인·쌍둥이 보너스·동점 도달 순.
- 개인·일부 대상: 이름이 유일하면 **이름**으로 저장(기존 방식). **동명이인은 `이름|학교|학년` 토큰**으로 저장 —
  student-picker가 자동 처리, 백엔드 noticeMatches_가 학교(느슨)·학년까지 대조 (2026-07: 1·2학년 이서윤 분석지 혼입 건 수정).
  화면 표시는 '이름(학교 학년)'. ⚠️ 수정 전(이름만)에 저장된 동명이인 배정은 재배정해야 분리됨.
- 명단은 `학생정보` 탭의 **재원 학생**만(K열 퇴원/N 등 제외). 학생ID(부모님 8자리)는 절대 노출 안 함.
- 비밀번호: 백엔드 `TEACHER_PW='sh'` / 교사 페이지 잠금도 **`sh`로 통일** (2026-07 — localStorage 키 `shueguk_teacher_unlocked` 공유, 이미 해제한 기기는 재입력 불필요). 어휘·문법 대시보드의 `shueguk2026`은 별개(API 키).
- 정적 파일(html/js/css)은 머지만 하면 1~2분 뒤 반영(강력 새로고침). 백엔드(.gs)는 재배포 필요.
